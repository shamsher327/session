{#
/**
 * @file
 * Default theme implementation to display a paragraph.
 *
 * Available variables:
 * - paragraph: Full paragraph entity.
 *   Only method names starting with "get", "has", or "is" and a few common
 *   methods such as "id", "label", and "bundle" are available. For example:
 *   - paragraph.getCreatedTime() will return the paragraph creation timestamp.
 *   - paragraph.id(): The paragraph ID.
 *   - paragraph.bundle(): The type of the paragraph, for example, "image" or "text".
 *   - paragraph.getOwnerId(): The user ID of the paragraph author.
 *   See Drupal\paragraphs\Entity\Paragraph for a full list of public properties
 *   and methods for the paragraph object.
 * - content: All paragraph items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - paragraphs: The current template type (also known as a "theming hook").
 *   - paragraphs--type-[type]: The current paragraphs type. For example, if the paragraph is an
 *     "Image" it would result in "paragraphs--type--image". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - paragraphs--view-mode--[view_mode]: The View Mode of the paragraph; for example, a
 *     preview would result in: "paragraphs--view-mode--preview", and
 *     default: "paragraphs--view-mode--default".
 * - view_mode: View mode; for example, "preview" or "full".
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @see template_preprocess_paragraph()
 *
 * @ingroup themeable
 */
#}

{{ kint(paragraph) }}

{% block paragraph %}
{% set component_title=content.field_title[0]['#context']['value'] %}
{% set video_url=file_url(content.field_video[0]['#media'].field_media_video_file.entity.uri.value) %}
{% set component_image_title=content.field_image['#items'].alt %}
{% set hero_variations=content.field_indegene_variations.0['#markup'] %}
{% set landing_page_url=file_url(content.field_image.0['#item'].entity.uri.value) %}
{% set landing_page_cta_title1=content.field_cta.0['#title'] %}
{% set landing_page_cta_url1=content.field_cta.0['#url'] %}
{% set landing_page_cta_title2=content.field_cta.1['#title'] %}


{% if( hero_variations == 'Indegene Variation') %}
<section class="bg-gray-lighter page-section bg-scroll home-slider page-" style="background-color: #0a4ca4; height: auto; padding-top: 0px; padding-bottom: 0px;">
    <div class="row">
        <div class="col-md-5 slider_leftsection">
            <h2 class="align-left font-alt white hs-line-14 mb-30 fw-800 blue desktop-only" style="margin-top: 0px;">{{component_title}}
            </h2>
            </div>
            <div class="col-sm-7 desktop-only home-image">
                <video autoplay="" loop="" muted="" playsinline=""><source src={{video_url}}></video>
        </div>
    </div>
</section>

{% endif %}

{% if hero_variations == 'Carrier Variation' %}
<div class=" fadeInUp container  wow mt-100 mb-100">
    <section class=" bg-dark page-section bg-scroll" style="background-color: #f9f9f9; padding-top: 0px; padding-bottom: 0px;" id="career_div">
        <div class=" row  homecareer_div">
            <div class=" col-sm-6 col-md-6 small-section-2 f-right">
                <h3 class=" align-left font-alt black fw-700" style="margin-top: 0px; margin-bottom: 15px;">{{component_image_title}}
                </h3>
                <a class=" btn btn-mod btn-gray btn-small btn-round" target="_blank" style="margin-top: 10px; margin-right: 40px;" href="/careers/life-indegene"><i class="fa fa-caret-right"></i> {{landing_page_cta_title1}}</a>
                <a class=" btn btn-mod btn-gray btn-small btn-round" target="_blank" style="margin-top: 10px;" href="https://careers.indegene.com/"><i class="fa fa-caret-right"></i> {{landing_page_cta_title2}}</a>
            </div>
            <div class=" col-sm-6 col-md-6 hwd_img_2" style="background-image: url({{landing_page_url}});">
            </div>
        </div>
    </section>
</div>
{% endif %}

{% endblock paragraph %}

