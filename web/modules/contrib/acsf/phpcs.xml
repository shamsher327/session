<?xml version="1.0"?>
<ruleset name="ACSF">
  <description>This file is mostly for phpcs local usage; Travis (ORCA) ignores it.</description>

  <!--
  Include everything to start.
  -->
  <file>.</file>

  <!--
  Exclude paths with contrib code.
  -->
  <exclude-pattern>vendor/*</exclude-pattern>
  <exclude-pattern>*.md</exclude-pattern>

  <rule ref="AcquiaDrupalTransitional" />
</ruleset>
