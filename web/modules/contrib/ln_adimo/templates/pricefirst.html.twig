{#
/**
 * @file
 * Template file for adimo module.
 */
#}
{% set data_cookie_consent= cookieconsent ? "false" : "true" %}
{% if touchpointId is not empty %}

  {{ attach_library('ln_adimo/pricefirst') }}
  {{ attach_library('ln_adimo/adimo-cookie-consent') }}

  <!-- To be placed where you would like the button to appear -->
  {% if customButtonHTML is not empty %}
    <a class="adimo-site-product-first" data-type="site-product-first"
       data-static-touchpoint-identifier="{{ touchpointId }}"
       data-do-not-track="false"
       onclick="window.Adimo.setLanguage('{{ language }}');">{{ customButtonHTML|raw }}</a>
  {% else %}
    <a class="adimo-site-product-first" data-type="site-product-first"
       data-static-touchpoint-identifier="{{ touchpointId }}"
       data-do-not-track="false">
      <button class="button-text"
              onclick="window.Adimo.setLanguage('{{ language }}');">{{ 'Buy Now'|t }}</button>
    </a>
  {% endif %}

  {% if customCSS is not empty %}

    <style>{{ customCSS }}</style>

  {% endif %}
{% endif %}
