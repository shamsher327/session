ln_quiz.process:
  path: '/quiz/{quiz}/{action}'
  defaults:
    _controller: '\Drupal\ln_quiz\Controller\QuizController::process'
  requirements:
    _permission: 'access content'
  options:
    parameters:
      quiz:
        type: entity:paragraph
